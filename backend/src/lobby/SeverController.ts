import { Socket } from 'socket.io';

// export interface Controller {
//   on(event: string, callback: (socket: Player, ...args: any[]) => void): void;

//   emit(event: string, ...args: any[]): void;
// }

// export class ServerController {}
